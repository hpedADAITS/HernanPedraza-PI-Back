const songsSchema = require('./songs.schema');
const eventsSchema = require('./events.schema');
const votesSchema = require('./votes.schema');
const participantsSchema = require('./participants.schema');
const friendsSchema = require('./friends.schema');
const authSchema = require('./auth.schema');

module.exports = {
  songsSchema,
  eventsSchema,
  votesSchema,
  participantsSchema,
  friendsSchema,
  authSchema,
};
