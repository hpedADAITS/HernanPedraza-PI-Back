const authValidator = require("./auth.validator");
const eventsValidator = require("./events.validator");
const songsValidator = require("./songs.validator");
const participantsValidator = require("./participants.validator");
const votesValidator = require("./votes.validator");

module.exports = {
  authValidator,
  eventsValidator,
  songsValidator,
  participantsValidator,
  votesValidator,
};
